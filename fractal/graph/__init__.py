# fractal.graph — Document graph construction for the fractal system.
#
# Builds Level 2 of the fractal graph: intra-document structure edges.
#
# Submodules:
#   document_graph.py   — containment + sequence edges from parsed chunks
#   cross_reference.py  — intra-document reference edges (Table N, Figure N)
