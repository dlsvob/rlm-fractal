# fractal — Top-level package for the rlm-fractal graph system.
#
# This package organizes the fractal knowledge graph into submodules:
#   fractal.parser    — PDF parsing pipeline (Phase 2)
#   fractal.graph     — Document graph construction (Phase 3)
#   fractal.query     — Recursive query engine (Phase 4)
